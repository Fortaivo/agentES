{
  "name": "agentes",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "npm run dev --workspace @agentes/web",
    "dev:api": "npm run dev --workspace @agentes/api",
    "dev:worker": "npm run dev --workspace @agentes/worker",
    "dev:mcp": "npm run dev --workspace @agentes/mcp-server",
    "build": "npm run build --workspace @agentes/contracts && npm run build --workspace @agentes/api && npm run build --workspace @agentes/worker && npm run build --workspace @agentes/mcp-server && npm run build --workspace @agentes/web",
    "build:web": "npm run build --workspace @agentes/web",
    "build:api": "npm run build --workspace @agentes/api",
    "build:worker": "npm run build --workspace @agentes/worker",
    "build:mcp": "npm run build --workspace @agentes/mcp-server",
    "typecheck": "npm run typecheck --workspace @agentes/contracts && npm run typecheck --workspace @agentes/api && npm run typecheck --workspace @agentes/worker && npm run typecheck --workspace @agentes/web",
    "typecheck:web": "npm run typecheck --workspace @agentes/web",
    "test:e2e": "playwright test",
    "test:mcp": "npm run build:mcp && node scripts/mcp-smoke.mjs",
    "test:qa": "npm run test:mcp && npm run test:e2e",
    "compose:up": "docker compose up --build",
    "compose:down": "docker compose down -v"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
